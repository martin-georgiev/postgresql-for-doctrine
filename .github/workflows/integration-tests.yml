name: Integration Tests

on:
  push:
    branches:
      - main
    paths-ignore:
      - '.github/actions/release-please/**'
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  should-run:
    runs-on: ubuntu-latest
    outputs:
      run: ${{ steps.check.outputs.run }}
    steps:
      - name: Skip for release-please
        id: release-please-check
        run: |
          if [ "${{ github.event.pull_request.user.id }}" = "41898282" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "::notice::Skipping integration tests - this is a release-please bot's interaction"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Checkout for file change detection
        if: steps.release-please-check.outputs.skip != 'true'
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          fetch-depth: 0

      - name: Check for src changes
        if: steps.release-please-check.outputs.skip != 'true'
        id: src-changes
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            BASE_SHA="${{ github.event.before }}"
            HEAD_SHA="${{ github.sha }}"
          else
            BASE_SHA="${{ github.event.pull_request.base.sha }}"
            HEAD_SHA="${{ github.event.pull_request.head.sha }}"
          fi

          CHANGED_FILES=$(git diff --name-only "$BASE_SHA" "$HEAD_SHA" 2>/dev/null || echo "")

          if [ -z "$CHANGED_FILES" ]; then
            echo "src_changed=true" >> $GITHUB_OUTPUT
            echo "::notice::Could not determine changed files, running integration tests"
          elif echo "$CHANGED_FILES" | grep -q "^src/"; then
            echo "src_changed=true" >> $GITHUB_OUTPUT
            echo "::notice::Source files changed, integration tests will run"
          elif echo "$CHANGED_FILES" | grep -q "^tests/Integration/"; then
            echo "src_changed=true" >> $GITHUB_OUTPUT
            echo "::notice::Integration test files changed, integration tests will run"
          elif echo "$CHANGED_FILES" | grep -q "^\.github/"; then
            echo "src_changed=true" >> $GITHUB_OUTPUT
            echo "::notice::GitHub workflow files changed, integration tests will run"
          elif echo "$CHANGED_FILES" | grep -q "^composer\.json$"; then
            echo "src_changed=true" >> $GITHUB_OUTPUT
            echo "::notice::composer.json changed, integration tests will run"
          else
            echo "src_changed=false" >> $GITHUB_OUTPUT
            echo "::notice::No relevant changes detected, skipping integration tests"
          fi

      - name: Determine if tests should run
        id: check
        run: |
          if [ "${{ steps.release-please-check.outputs.skip }}" = "true" ]; then
            echo "run=false" >> $GITHUB_OUTPUT
          elif [ "${{ steps.src-changes.outputs.src_changed }}" = "false" ]; then
            echo "run=false" >> $GITHUB_OUTPUT
          else
            echo "run=true" >> $GITHUB_OUTPUT
          fi

  integration-tests:
    needs: should-run
    if: needs.should-run.outputs.run == 'true'
    runs-on: ubuntu-latest
    name: "PostgreSQL ${{ matrix.postgres }} + PostGIS ${{ matrix.postgis }} + PHP ${{ matrix.php }}"

    strategy:
      fail-fast: ${{ github.event_name == 'pull_request' }}
      matrix:
        php: ['8.2', '8.3', '8.4', '8.5']
        postgis: ['3.4', '3.5', '3.6']
        postgres: ['16', '17', '18']
        exclude:
          # PostgreSQL 16: Docker images support only for PostGIS 3.4, 3.5
          - postgres: '16'
            postgis: '3.6'
          # PostgreSQL 18: Docker images support only for PostGIS 3.6
          - postgres: '18'
            postgis: '3.4'
          - postgres: '18'
            postgis: '3.5'
        include:
          # Code coverage on the latest stable combination
          - php: '8.5'
            postgres: '18'
            postgis: '3.6'
            calculate-code-coverage: true

    services:
      postgres:
        image: postgis/postgis:${{ matrix.postgres }}-${{ matrix.postgis }}-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: postgres_doctrine_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
          -e POSTGRES_INITDB_ARGS="--data-checksums"

    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6

      - name: Set up PHP
        uses: shivammathur/setup-php@44454db4f0199b8b9685a5d763dc37cbf79108e1 # v2
        with:
          php-version: ${{ matrix.php }}
          coverage: ${{ matrix.calculate-code-coverage == true && 'xdebug' || 'none' }}
          extensions: ctype, json, mbstring, pdo_pgsql
          tools: composer

      - name: Validate composer.json and composer.lock
        run: composer validate --strict

      - name: Get composer cache directory
        id: composer-cache-dir
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache Composer packages
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5
        with:
          path: |
            ${{ steps.composer-cache-dir.outputs.dir }}
            vendor
          key: ${{ runner.os }}-php-${{ matrix.php }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-php-${{ matrix.php }}-composer-
            ${{ runner.os }}-composer-

      - name: Install dependencies
        run: composer install --prefer-dist --no-interaction --no-progress

      - name: Install PostgreSQL client tools
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: Verify PostgreSQL connection and setup
        run: |
          PGPASSWORD=postgres psql -h localhost -U postgres -d postgres_doctrine_test -c "CREATE SCHEMA IF NOT EXISTS test; SELECT PostGIS_Version();"

      - name: Run integration test suite
        run: |
          if [ "${{ matrix.calculate-code-coverage }}" = "true" ]; then
            XDEBUG_MODE=coverage composer run-integration-tests
          else
            composer run-integration-tests -- --no-coverage
          fi
        env:
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_DB: postgres_doctrine_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres

      - name: Upload coverage results to Coveralls
        if: matrix.calculate-code-coverage == true
        uses: coverallsapp/github-action@5cbfd81b66ca5d10c19b062c04de0199c215fb6e # v2
        env:
          COVERALLS_SERVICE_NAME: github
          COVERALLS_SERVICE_NUMBER: ${{ github.sha }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          file: ./var/logs/test-coverage/integration/clover.xml
          parallel: true
          flag-name: "Integration"
          fail-on-error: false
